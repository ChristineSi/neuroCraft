class MockModel:
    def predict(self, text):
        # This is a placeholder method; replace it with your actual prediction logic
        # For simplicity, let's assume it always predicts 'easy' for demonstration purposes
        return 'hard'

# Instantiate the mock model
mock_model = MockModel()
